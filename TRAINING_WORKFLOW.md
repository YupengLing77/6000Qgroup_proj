# Qwen3-VL Logo 识别微调完整流程

## 📋 一次性设置（已完成）✅

数据已转换完成，共 126,923 张训练图像，无需重复转换！

---

## 🚀 训练流程（以后每次训练）

### 步骤 1: 选择训练数据量

编辑 `prepare_data.py` 第 93 行，选择数据规模：

```python
# 小规模测试（1K图，15分钟）
prepare_training_data(num_train_images=1000, num_test_images=200)

# 中等规模（10K图，2-3小时）推荐
prepare_training_data(num_train_images=10000, num_test_images=1000)

# 全量数据（127K图，20-30小时）
prepare_training_data(num_train_images=126923, num_test_images=5000)
```

运行：
```bash
python prepare_data.py
```

生成文件：
- `train_subset.json` - 训练数据
- `test_subset.json` - 测试数据

### 步骤 2: 开始训练

```bash
bash train_logo_lora.sh
```

训练配置会自动使用 `train_subset.json`

### 步骤 3: 综合评估

训练完成后，运行完整评估（包含分类、检测、行业识别）：

```bash
python comprehensive_eval.py
```

生成报告：
- `evaluation_results/evaluation_table.csv` - **表格数据源**
- `evaluation_results/evaluation_report.md` - Markdown 报告
- `evaluation_results/results.json` - 详细结果

---

## 📊 实验记录模板

### 实验 1: 1K 数据（基线）

| 任务 | 指标 | 基础模型 | LoRA模型 | 提升 |
|------|------|---------|---------|------|
| 分类 | 准确率 | - | - | - |
| 检测 | IoU | - | - | - |
| 行业识别 | 准确率 | - | - | - |

**配置**:
- 训练图像: 1,000
- 训练时间: ~15分钟
- LoRA rank: 64
- 学习率: 1e-5

### 实验 2: 10K 数据

| 任务 | 指标 | 基础模型 | LoRA模型 | 提升 |
|------|------|---------|---------|------|
| 分类 | 准确率 | - | - | - |
| 检测 | IoU | - | - | - |
| 行业识别 | 准确率 | - | - | - |

### 实验 3: 全量数据

| 任务 | 指标 | 基础模型 | LoRA模型 | 提升 |
|------|------|---------|---------|------|
| 分类 | 准确率 | - | - | - |
| 检测 | IoU | - | - | - |
| 行业识别 | 准确率 | - | - | - |

---

## 🔄 快速切换数据规模

**不需要重新转换数据！** 只需：

1. 修改 `prepare_data.py` 中的数量
2. 运行 `python prepare_data.py`
3. 运行 `bash train_logo_lora.sh`

---

## 📁 文件说明

| 文件 | 用途 | 需要修改 |
|------|------|----------|
| `logo_train.json` | 全量训练数据（126K图） | ❌ 永久保存 |
| `logo_test.json` | 全量测试数据（31K图） | ❌ 永久保存 |
| `prepare_data.py` | 选择训练数据量 | ✅ 每次训练前 |
| `train_subset.json` | 当前训练子集 | ❌ 自动生成 |
| `test_subset.json` | 当前测试子集 | ❌ 自动生成 |
| `comprehensive_eval.py` | 综合评估脚本 | ❌ 直接运行 |

---

## 🎯 当前实验计划

### 第一轮：验证效果（当前）

已完成：
- ✅ 数据转换（126K图）
- ✅ 1K数据训练
- ✅ 基础评估

待完成：
- ⏳ 10K数据训练
- ⏳ 综合评估（三个任务）
- ⏳ 记录实验数据

### 第二轮：优化参数

- 调整 LoRA rank (32, 64, 128)
- 调整学习率 (5e-6, 1e-5, 2e-5)
- 使用全量数据

---

## 💡 提示

1. **数据已转换完成**，无需重复转换
2. **每次实验**只需运行 `prepare_data.py` 选择数量
3. **评估结果**会自动保存为表格，可直接用于论文
4. **三个任务**都会被评估：分类、检测、行业识别
